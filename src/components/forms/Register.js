import React from "react";
// add logic to redirect from here if user logged in

class Register extends React.Component {
  render() {
    return (
      <div>
        <h1>Register now!</h1>
      </div>
    );
  }
}

export default Register;
